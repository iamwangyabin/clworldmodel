# AWM-AutoRoute v4: task-0 seed cross-evaluation diagnostic

User authorized this independent evaluation on 2026-09-08. Classification:
**debug**, not a new formal score, reproduction, ablation training, or weight
selection opportunity. No current training process or source tree is changed.

## Fixed matrix, before observing its results

Use S0 (123456789) and S1 (1337), both trained from commit
`12d317eaf98eaebeb462894894b6fc83bb5e7041`, at the **first completed task
boundary, 90 epochs / 92,000 WM updates / 72,000 AC updates**. These inference
snapshots are after boundary consolidation/compression, before schedule advance;
record each actual compression layout. Never substitute a latest, best, v3, or
partially trained checkpoint if S1 is not ready.

Evaluate both frozen world-model/actor pairs on both existing task-0 periodic
validation seeds: **2127245496 (S0 cohort)** and **1981893629 (S1 cohort)**.
This yields four cells. Do not use either final held-out cohort. All cells run
sequentially on **4090-1 GPU 0**, eight CPU threads, using the same audit commit,
runtime, BF16 compute / FP32 master parameters and TF32-high setting. Training
on either host continues independently. Extra evaluation may temporarily contend
for compute but adds no training data or updates.

## Evaluation invariants

Reuse the unchanged vendored Atari `evaluate` function, including its legacy
NextStep reset/reward/episode accounting: four environments, repeat four,
16 nominal rollouts (actual completed count may differ), deterministic latent
mode and argmax policy. MsPacman reward scale remains 0.05; raw per-task return
is scaled return / 0.05. No human-normalized score or different paper metric is
substituted. Eligible route registry is `[0]`, with no task ID input to policy;
single-candidate routing cannot diagnose multi-task routing accuracy.

A transparent observer saves the exact tensors returned by the existing
generator without changing them. They preserve the legacy packed/truncated
layout, not a corrected episode dataset. `returned_tensor_samples` describes
those tensors, **not an exact count of ALE frames**. The existing evaluator's
nominal decision cap is 32,768 per cell, repeat four; no additional episodes
are generated by the observer. Total budget is four cells / 64 nominal rollouts,
zero updates, zero Replay capacity/bytes. Never merge these into formal metrics.

Strictly restore model state, freeze parameters, set evaluation mode, and
verify all parameters/buffers and the source snapshot remain unchanged. Save
snapshot SHA-256, full training commit, separately fetched/synchronized audit
commit, resolved training config, runtime/backend settings, routing events,
actual episode count, raw/scaled mean/std, tensor SHA-256 and elapsed time.

```bash
PYTHONPATH=src:tests:scripts python -m unittest test_awm_seed_cross_eval
CUDA_VISIBLE_DEVICES=0 OMP_NUM_THREADS=8 MKL_NUM_THREADS=8 \
  python scripts/awm_seed_cross_eval.py --seed-index 0 \
  --snapshot /absolute/path/boundary_01_task_00_completed_0090.pt \
  --snapshot-sha256 VERIFIED_SHA256 --output-dir /absolute/new/diagnostic_s0
```

Run index 1 with its own verified immutable snapshot and a new output directory.
Both launches must use clean already-pushed code. The launcher fetches and
rejects dirty/ahead/behind state before environment interaction.

## Interpretation limits

Compare rows at a fixed cohort to measure the trained model-pair difference;
compare columns for a fixed model to measure cohort sensitivity. This small
matrix does **not** isolate weight initialization from exploration/Replay,
world model from actor, hardware effects during training, or establish the
variance over a population of training seeds. Neither selecting the favorable
cell nor treating episodes as independent training seeds is permitted.
